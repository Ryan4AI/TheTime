# 2026-06-16 工作日记

## 01:08-01:24 — v0.6.35/36: 属性评分分离 AI₂ + 榜单前端展示

### 架构大改：庇护/危险度移除 + 属性评分独立

**三大改动：**

1. **去掉庇护+危险度**
   - Prompt 删除"死神危险度""历史庇护"两段，改为精简"属性参考"
   - state/identity 删除"历史庇护"默认值
   - game.js 删除 drawDangerIndicator（~100 行红色脉冲逻辑）

2. **属性评分拆分独立 AI₂**
   - AI₁ (叙事) 的 patch 不再含属性变化
   - 新增 callScoringAI() 每轮剧情后独立评估 9 属性增量
   - 评分 AI 专用 prompt：输入剧情+当前属性 → 输出 JSON 属性增量
   - 失败保底全 0，不阻塞叙事
   - emitSystemMessages 不变（仍检测前后 state 差异）

3. **AI₁ 感知属性 + 榜单接近度**
   - prompt 状态段显示全部 9 属性值
   - 新增 BOARD_THRESHOLDS + calcBoardScore + computeClosestBoard
   - prompt 行："榜单接近度：名医榜（还差150分）"

### 前端改动
- 关键 bug 修复：前端之前从 `branch.patch` 读属性变化，patch 不再含属性 → 从 `newState` 读
- "榜"按钮移出右上角：`min(windowW-60, windowW×0.65)`，避开微信三点菜单
- 新增 drawBoardTarget()：状态栏下方显示"🎯 名医榜 还差150分"

### 其他
- import_leaderboards 修改：用 doc().set() 替代 add() 以保留 _id
- 10 榜 790 位历史人物成功导入云数据库

## 01:45-01:52 — v0.6.37: 榜单目标加强 + 浮窗关闭修复

- **榜单目标显示升级**：`🏆 名医榜  超越孔伯华(民国)  还差150分`（含具体目标人物、圆角面板）
- 新增 BOARD_TARGET_PERSON 硬编码（各榜末位人物：孔伯华/林冲/伍崇曜/黄景仁/赵高/王光兴/关汉卿/岳飞）
- **榜单浮窗修复**：点击任意位置关闭（之前只有关闭按钮有效，其他区域点击无反应）
- **部署 leaderboard_query 云函数**：首次部署（之前只写了代码没部署过），用于榜单数据查询
- 部署坑：`tcb fn deploy` 首次部署需要交互式选 runtime，用 `yes "y" | npx tcb fn deploy` 绕过了确认环节
